# Frozen PID Baseline

This folder is a snapshot of the pre-RL visual-servoing baseline.

Use the active simulator with `--control-mode pid` for normal baseline runs:

```bash
cd scripts
python main.py --control-mode pid
```

Files in this folder should not be edited while developing the RL controller.
